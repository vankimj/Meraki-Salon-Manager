rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /headshots/{file} {
      allow read: if true;
      allow write, delete: if request.auth != null &&
        request.auth.token.email in ['jvankim@gmail.com'];
    }
  }
}
